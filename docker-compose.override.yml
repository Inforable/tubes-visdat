version: '3.8'

services:
  app:
    ports:
      - "8501:8501"
    volumes:
      - ./:/app
    env_file:
      - .env
    environment:
      - STREAMLIT_SERVER_RUN_ON_SAVE=true
    command: ["streamlit", "run", "app.py", "--server.runOnSave=true"]
